services:
  navidrome:
    image: deluan/navidrome:latest
    container_name: navidrome
    ports:
      - "4533:4533"
    restart: unless-stopped
    env_file: ".env"
    volumes:
      - "~/Documents/prog/navidrome/data:/data"
      - "~/Music:/music:ro"

