# Keeping this here lets vim set the ft to 'conf' which enables commenting

@author "Dustin Pilgrim"
@description "Lightweight feature packed idle manager for Wayland"

default:
  enable_loginctl_integration false
  enable_dbus_inhibit true
  pre_suspend_command None
  monitor_media true
  ignore_remote_media true

  # Media sources that allow lock/DPMS but block automatic suspend while playing.
  suspend_inhibit_media [ ]
  debounce_seconds 4
  notify_on_unpause true
  notify_before_action true

  # Laptop Options
  lid_close_action "qs -p ~/.config/quickshell/lockscreen.qml && niri msg action power-off-monitors"
  lid_open_action "niri msg action power-on-monitors"

  # Apps that inhibit Stasis while running (regex allowed via r"...")
  inhibit_apps [
    "mpv"
    "vlc"
    "Spotify"
    r".*\.exe"
    r"steam_app_.*"
    r"firefox.*"
    r"librewolf.*"
  ]

  # App/process patterns that block only automatic suspend.
  suspend_inhibit_apps [ ]

  # Desktop Plan
  early_dpms:
    timeout 300
    command "niri msg action power-off-monitors"
    resume_command "niri msg action power-on-monitors"
  end

  lock_screen:
    timeout 300
    command "qs -p ~/.config/quickshell/lockscreen.qml"
    notification "Locking session in 10s"
    notify_seconds_before 10
  end

  dpms:
    timeout 60
    command "niri msg action power-off-monitors"
    resume_command "niri msg action power-on-monitors"
  end

  # Charging (Laptop-specific)
  ac:
    brightness:
      timeout 300 # 5 minute(s)
      command "brightnessctl set 25%"
    end

    dpms:
      timeout 120 # 2 minute(s) after brightness
      command "niri msg action power-off-monitors"
      resume_command "niri msg action power-on-monitors"
    end

    lock_screen:
      timeout 180 # 3 minute(s) after dpms
      command "qs -p ~/.config/quickshell/lockscreen.qml"
    end
  end

  # Discharging (Laptop-specific)
  battery:
    brightness:
      timeout 60 # 1 minute(s)
      command "brightnessctl set 30%"
    end

    dpms:
      timeout 120 # 2 minute(s) after brightness
      command "niri msg action power-off-monitors"
      resume_command "niri msg action power-on-monitors"
    end

    lock_screen:
      timeout 60 # 1 minute(s) after dpms
      command "qs -p ~/.config/quickshell/lockscreen.qml"
    end
  end
end
