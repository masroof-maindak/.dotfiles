#!/usr/bin/env bash
# Open today's journal entry

TEXT_ED=${1:-$EDITOR}

cd $VAULT_DIR
today=$(date +'%Y-%m-%d')
fpath="03 - Journal/$(date +'%Y')/$(date +'%m - %b')/${today}.md"
if [ -f "$fpath" ]; then
    $TEXT_ED "$fpath"
else
    echo "Note file doesn't exist."
fi
cd - >/dev/null
