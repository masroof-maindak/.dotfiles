#!/usr/bin/env bash

declare -A actions=(
    ["Lock screen"]="qs -p ~/.config/quickshell/lockscreen.qml"
    ["Log out // Quit Niri"]="niri msg action quit"
    ["Reboot // Restart"]="reboot"
    ["Shutdown // Poweroff"]="poweroff"
)

SELECTION=$(printf "%s\n" "${!actions[@]}" | fuzzel --placeholder="Power Menu" --dmenu -l 4) || exit 0
eval "${actions[$SELECTION]}"
