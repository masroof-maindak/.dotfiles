#!/usr/bin/env fish
# Find *.md files in Obsidian vault

cd $VAULT_DIR
set -l files (
    fd -t f -e md -E '.*' | _fzf --prompt "Vault: " --bind "ctrl-d:change-query($(date +'%Y-%m-%d'))"
)

if [ -n "$files" ]
    echo "$files" | xargs -d "\n" $EDITOR
end
cd - >/dev/null